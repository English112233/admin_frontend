let titlePlugin = {}
titlePlugin.install = function (Vue, options) {
    Vue.mixin({
        created: function () {
            if (this.$data['title'] && this.$data['useTitlePlugin'] !== false) {
                document.title = this.$data['title']
            }
        }

    })
}
export default titlePlugin
