'use strict';

Object.defineProperty(exports, "__esModule", {
    value: true
});

var _bluesdkUrl = require('bluesdk-url');

var _bluesdkUrl2 = _interopRequireDefault(_bluesdkUrl);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

/**
 * 获取vue风格的uri
 */
_bluesdkUrl2.default.getUri = function (url) {
    if (!url) url = location.href;
    var m = url.match(/#([^?]+)/);
    return m ? m[1] : null;
};

/**
 * 当前访问URL 不带域名
 */
_bluesdkUrl2.default.getCurrentUrl = function () {
    return this.getUri() + '?' + this.getQueryStr();
};

_bluesdkUrl2.default.replaceUrl = function (newQuery) {
    return '#' + this.getUri() + '?' + this.queryToStr(this.replaceQuery(newQuery));
};

exports.default = _bluesdkUrl2.default;