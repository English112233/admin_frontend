import url from 'bluesdk-url';

export default {
  serverUrl: '/api',
  tokenUrl: this.serverUrl + '/common/getToken.action',
  uploadUrl: function uploadUrl() {
    return window.location.protocol + '//upload.' + url.getRootDomain() + '/index.php';
  },
  remitOrderExcel: function remitOrderExcel() {
    return window.location.protocol + '//file.' + url.getRootDomain() + '/doc/foreignRemitOrderExcel.xls';
  }
};