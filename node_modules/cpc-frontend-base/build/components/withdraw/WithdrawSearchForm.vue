<template>
  <el-form :inline="true" class="query-form">
    <slot name="extend-first"></slot>
    <date-range :begin-value="dateBegin"
                :end-value="dateEnd"
                :common-range="commonDateRange"
                @updateBeginValue="$emit('update:dateBegin',$event)"
                @updateEndValue="$emit('update:dateEnd',$event)"
                @updateCommonRange="$emit('update:commonDateRange',$event)"
    >
    </date-range>
    <slot name="extend"></slot>
    <el-form-item label="交易状态" prop="sucFlag">
      <enum-select :options="tradeStatusEnum"
                   :show-empty="true"
                   :selected-id="sucFlag"
                   @change="val=>this.$emit('update:sucFlag', val)" style="width: 130px;"></enum-select>
    </el-form-item>

    <el-form-item>
      <el-button type="primary" @click="search()">查询</el-button>
    </el-form-item>
    <!--按钮插槽-->
    <slot name="extend-btn"></slot>
  </el-form>
</template>
<script>
  import { EnumSelect, DateRange } from 'bluesdk-element-common'
  import TradeStatusEnum from '../../enum/TradeStatusEnum'
  // import { TradeStatusEnum } from 'cpc-frontend-base'

  export default {
    name: 'WithdrawSearchForm',
    data () {
      return {
        tradeStatusEnum: TradeStatusEnum.map
      }
    },
    props: {
      'dateBegin': String,
      'dateEnd': String,
      'commonDateRange': String,
      'sucFlag': String
    },
    components: { DateRange, EnumSelect },
    methods: {
      search () {
        this.$emit('search')
      }
    }
  }
</script>
